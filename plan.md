# Plan: 外部休息信息文件编辑器

## 需求
- 在偏好设置页的"使用外部休息信息文件"区域添加"编辑"按钮
- 点击按钮打开表格式编辑器窗口，编辑对应的外部 JSON 文件
- 编辑器支持：添加/删除/启用/禁用条目，保存时校验格式和特殊字符
- 保存时自动写入 JSON 文件并重载 ideas

## 步骤

### [x] 步骤 1: 创建编辑器页面 HTML
- 新建 `app/ideas-editor.html`
- 微休息编辑器：表格，每行一个文本输入框 + 启用复选框 + 删除按钮
- 长休息编辑器：表格，每行两个文本输入框(标题+正文) + 启用复选框 + 删除按钮
- 底部：添加按钮 + 保存按钮 + 取消按钮
- 验证: HTML 结构正确

### [x] 步骤 2: 创建编辑器 preload
- 新建 `app/ideas-editor-preload.mjs`
- 暴露 ideas 操作 IPC: 读取文件、保存文件
- 验证: preload 导出正确

### [x] 步骤 3: 创建编辑器渲染器 JS
- 新建 `app/ideas-editor-renderer.js`
- 加载文件内容渲染为表格行
- 添加/删除/启用/禁用行
- 保存时校验数据（非空、无特殊字符）
- 调用 IPC 保存文件
- 验证: 逻辑正确

### [x] 步骤 4: main.js 添加 IPC handler 和窗口创建
- `read-ideas-file`: 读取外部文件内容
- `save-ideas-file`: 校验 + 写入文件 + 重载 ideas
- 创建编辑器窗口的函数
- 验证: IPC 通信正常

### [x] 步骤 5: 偏好设置页添加"编辑"按钮
- 在路径输入框旁加"编辑"按钮
- 点击按钮发送 IPC 打开编辑器窗口
- 验证: 按钮可点击并打开编辑器

### [x] 步骤 6: 添加 i18n 翻译
- en.json / zh-CN.json 添加编辑器相关翻译
- 验证: 翻译 key 完整

### [x] 步骤 7: lint + 测试验证
- npm run lint 零警告
- 相关测试通过 (65 passed)
